apply plugin: 'application'

mainClassName = "stan.server.sample.App"

sourceCompatibility = 1.8
targetCompatibility = 1.8

sourceSets {
    main {
        resources {
            srcDirs "src/main/res"
        }
    }
}

dependencies {
    compile project(':server')
}
